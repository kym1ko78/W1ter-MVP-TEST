## Goal
Разрешить merge conflict в [auth-screen.tsx](C:\Users\User\Desktop\Project\apps\web\components\auth-screen.tsx) без потери рабочей логики формы и без отката последних визуальных правок auth-экрана.

## Assumptions
- Конфликт возник между вашей веткой и `main` в одном и том же участке формы авторизации.
- Верхняя часть экрана уже содержит нужные визуальные правки:
  - заголовок `Вход в WR`
  - бейдж `Alfa`
  - обновленную композицию auth-экрана
- В `main` есть обязательные рабочие поля формы:
  - `Email`
  - `Пароль`
  - блок ошибки
- В вашей ветке дополнительно есть новая функциональность:
  - `rememberMe`
  - чекбокс `Запомнить меня на этом устройстве`
- Конфликт нужно решать не выбором одной стороны целиком, а сборкой объединенной финальной версии.

## Plan
### Phase 1: Удалить конфликтные маркеры и выделить две версии
- Найти и убрать из файла все служебные маркеры Git:
  - `<<<<<<< feat/profile-avatar`
  - `=======`
  - `>>>>>>> main`
- Разделить содержимое конфликта на два смысловых блока:
  - что пришло из вашей ветки
  - что пришло из `main`
- Не пытаться сохранить маркеры в коде: файл должен остаться обычным валидным TSX.
- Output / exit criterion:
  - понятно, какие куски относятся к каждой стороне конфликта

### Phase 2: Зафиксировать, что нужно сохранить обязательно
- Оставить верхние визуальные правки auth-экрана:
  - `Вход в WR`
  - `Alfa`
  - обновленную структуру экрана
- Оставить обязательные элементы формы из `main`:
  - поле `Email`
  - поле `Пароль`
  - `FieldError` для этих полей
  - блок `auth-error-message`
  - submit button
  - ссылку переключения между `login/register`
- Output / exit criterion:
  - собран список элементов, без которых форма работать не должна

### Phase 3: Принять решение по remember me
- Отдельно решить, оставляем ли новую функциональность:
  - `rememberMe` в schema
  - `rememberMe` в `defaultValues`
  - чекбокс `Запомнить меня на этом устройстве`
- Здесь возможны два сценария:
  - вариант A: оставляем `rememberMe` и в UI, и в типах, и в submit values
  - вариант B: временно убираем `rememberMe` целиком, если он еще не доведен
- В текущем коде по структуре видно, что `rememberMe` уже встроен в схемы и типы, поэтому базово безопаснее его сохранить, если backend/login flow это поддерживает.
- Output / exit criterion:
  - принято явное решение по `rememberMe`, чтобы не получить полусобранную форму

### Phase 4: Собрать итоговый блок формы
- Финальная форма должна выглядеть так по порядку:
  - `Имя` только для `register`
  - `Email`
  - `Пароль`
  - при необходимости `rememberMe`
  - блок ошибки
  - submit button
  - нижняя ссылка перехода между режимами
- Важно не дублировать:
  - блок ошибки
  - кнопку submit
  - закрывающие теги `</form>` и `</p>`
- Output / exit criterion:
  - есть один цельный валидный блок формы без дублирования и без пропусков

### Phase 5: Проверить логику login/register
- Убедиться, что:
  - `displayName` остается только для `register`
  - `rememberMe`, если сохраняется, доступен в обоих режимах или ровно там, где задумано
  - тексты кнопки:
    - `Создать аккаунт`
    - `Войти`
    продолжают корректно зависеть от `isRegister`
- Проверить, что после merge не исчезла логика:
  - `setStoredEmailVerificationPreviewUrl`
  - redirect на `/verify-email?sent=1`
  - redirect на `/chat`
- Output / exit criterion:
  - merge не ломает бизнес-логику auth-flow

### Phase 6: Провести синтаксическую и поведенческую валидацию
- После ручной сборки формы проверить, что TSX снова валиден:
  - нет незакрытых тегов
  - нет дублей submit-блока
  - нет маркеров конфликта
- Затем прогнать:
  - `pnpm --filter @repo/web typecheck`
  - `pnpm verify:web`
- Если форма затрагивает auth flow существенно, дополнительно стоит открыть `/login` и `/register` в dev и быстро проверить визуально.
- Output / exit criterion:
  - файл компилируется, экран входа и регистрации снова рабочий

## Что именно нужно выбрать
- Обязательно оставить:
  - `Email`
  - `Пароль`
  - блок ошибки
  - submit button
  - ваши визуальные правки верхней части auth-экрана
- Нужно принять решение:
  - оставляем ли `rememberMe` и чекбокс `Запомнить меня на этом устройстве`

## Recommended Resolution
- Рекомендую оставить обе стороны в объединенном виде:
  - сохранить поля `Email` и `Пароль` из `main`
  - сохранить `rememberMe` из вашей ветки
  - сохранить ваши визуальные правки auth-экрана
- То есть правильный merge здесь, скорее всего, не `ours` и не `theirs`, а ручная сборка общего варианта.

## Risks and Dependencies
- Риск: выбрать только верхнюю часть конфликта и случайно потерять `Email`/`Пароль`.
- Mitigation:
  - сначала собрать обязательные поля формы, потом добавлять `rememberMe`.

- Риск: выбрать только `main` и потерять `rememberMe`.
- Mitigation:
  - отдельно решить судьбу `rememberMe`, а не позволять Git решить это случайно.

- Риск: оставить оба submit-блока или оба error-блока.
- Mitigation:
  - после merge проверить, что в форме только по одному экземпляру каждого смыслового элемента.

## Next Actions
- Решить, оставляем ли `rememberMe`
- После этого вручную собрать финальный блок формы в [auth-screen.tsx](C:\Users\User\Desktop\Project\apps\web\components\auth-screen.tsx)
- Удалить conflict markers
- Прогнать `typecheck` и `verify:web`
